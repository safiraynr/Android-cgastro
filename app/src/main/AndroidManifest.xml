<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectbarudeh">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Akun_Pakar"></activity>
        <activity android:name=".Login_Pakar" />
        <activity android:name=".splashview">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tentang_Cgastro" />
        <activity android:name=".Tentang_Diare" />
        <activity android:name=".Read_Pakar" />
        <activity android:name=".dialog_view2" />
        <activity android:name=".Menu_Pakar" />
        <activity android:name=".riwayat_diagnosa" />
        <activity android:name=".Gejala" />
        <activity android:name=".CekDiagnosis" />
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>